/**
 * 자주 사용하는 연산
 */

// 배열 만들기

let fruits = ["apple", "banana"];
console.log(fruits.length); // 2
console.log("-----");

// 인덱스로 배열의 항목에 접근하기

let first = fruits[0];
console.log(first); // apple

let last = fruits[fruits.length - 1];
// 총 배열의 길이 - 1은 무조건 마지막 배열의 요소
console.log(last); // banana
